# 成员端移动多机型适配验收记录（2026-07-30）

状态：Ready for review。staging 人工验收已完成；审查、合并和发布记录尚未执行。

## 范围

- 成员端首页、切片、礼物、榜单、我的、成长与积分记录；投稿和资料弹窗。
- 登录、注册和找回密码页。
- 不包含管理后台、接口、数据库 migration 或积分规则。

## 自动化结果

| 引擎 | 视口 | 结果 |
| --- | --- | --- |
| Chromium | 320×568、360×800、390×844、412×915 | 通过 |
| Chromium | 568×320（短屏横向） | 通过 |
| WebKit | 375×812、390×844、430×932 | 通过 |

每个矩阵用例均校验页面根节点无横向溢出，成员壳、内容区、底部导航和打开的弹窗没有越出视口；同时覆盖主导航、成长/积分记录及登录三种表单模式。原有周挑战、成长加载/失败和投稿反馈的 `390×844` 用例也已通过。

## Staging 人工验收

- 已由用户确认完成。验收范围为真实 iPhone Safari 与 Android Chrome 的刘海/手势区、浏览器工具栏与软键盘，以及成员提交切片、兑换、转账、通知和空数据/错误/禁用状态。
- 待审查记录补充：staging 版本、验收人、截图位置、异常项（如有）和回滚点。

## 单人维护者自审

- 已核对本次变更未触及 API、数据库 migration、认证、RBAC 或积分规则；管理后台不在改动范围内。
- 已复核移动端不通过全局隐藏横向溢出掩盖问题，局部横滑仅保留给标签和筛选控件。
- 本地门禁已通过：类型检查、126 项单元/数据库集成测试、23 项 Playwright、生产构建、安全审计和 Docker 构建。
- 应用回滚点为合并前的 release commit；本次无 migration，应用版本可独立回退。
