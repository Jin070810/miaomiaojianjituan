# 2026-08-07 v1.9.0 正式发布记录

## 发布结果

- 状态：生产代码部署成功；生日基础入口已启用，生日奖励待奖池配置后启用
- 版本：`v1.9.0`
- 域名：`https://miaoyi.site`
- 正式提交：`b317f95c2d7aa57e3d489714ba712c23efc58e5f`
- 工作项：[#74](https://github.com/Jin070810/miaomiaojianjituan/issues/74)
- Pull Request：[#73](https://github.com/Jin070810/miaomiaojianjituan/pull/73)
- GitHub Release：[v1.9.0](https://github.com/Jin070810/miaomiaojianjituan/releases/tag/v1.9.0)
- main CI：[CI #31177018478](https://github.com/Jin070810/miaomiaojianjituan/actions/runs/31177018478)
- 发布 workflow：[Deploy Production #31177477219](https://github.com/Jin070810/miaomiaojianjituan/actions/runs/31177477219)
- 发布时间：2026-08-07 20:22:43（Asia/Shanghai）
- 发布人：Administrator（单人维护者）

## 变更摘要

- 新增生日登记、生日墙、预设祝福、年度抽奖、生日投稿积分加成、商品领奖、纪念卡和永久生日勋章。
- 首页与个人中心只提供固定入口，不返回或广播其他成员生日；成员主动进入生日墙后才查询今日寿星和未来 30 天生日。
- 首次进入生日页展示生日权益说明，查看状态由服务端保存；成员端不公开抽奖概率或策略版本。
- 完整生日加密保存，成员接口只返回月日；公开状态可立即关闭，资料修改具有 7 天生效和 365 天间隔约束。
- 抽奖、积分、商品预留库存、零积分领奖订单、个人通知与审计使用事务和幂等约束；生日改期不会重开已关闭的年度抽奖窗口。
- 新增 `BIRTHDAY_PROGRAM` 和 `BIRTHDAY_REWARDS` 开关，均默认关闭；Worker 不生成面向全体成员的生日通知。

## Migration、镜像与备份

- 新增 migration：`20260807090000_birthday_wishes`、`20260807100000_birthday_effective_at`、`20260807110000_birthday_prize_revoke`、`20260807120000_birthday_onboarding`。
- migration 只追加生日资料、年度权益、奖池、奖品、祝福、领奖与固化状态所需结构，不修改历史 migration，不回填生日数据；生产 `migrate` 容器成功退出。
- App digest：`sha256:fd333aa828f049fcc012ea4a6aa6716388f07801b13e5c96fb69fc16c913729f`。
- Worker digest：`sha256:2afede5344f6f9da6c712398b412c5fbf00ae3b981cfa5bd7d0a06fc66e2a7c8`。
- App/Worker OCI revision 均为 `b317f95c2d7aa57e3d489714ba712c23efc58e5f`。
- 发布前备份：`backups/miaomiao-20260807-122208.dump`。
- 校验文件：`backups/miaomiao-20260807-122208.dump.sha256`。
- 备份校验：成功（返回 `OK`）。
- 应用回滚点：`v1.8.5`（`b3f6a39e26e3c6b5500c847a45eabf40e428667f`）。
- 数据库回滚：不逆向删除四个追加 migration；关闭生日开关后同时回滚 App/Worker，结构问题使用前向 migration 修复。

## 审查、CI 与 staging

- #73 完成单人维护者自审，覆盖积分整数性、抽奖与领奖并发、库存、敏感生日、服务端 RBAC、migration、故障隔离和回滚边界。
- PR CI `31176326422` 与合并后 main CI `31177018478` 均通过。
- `npm run lint`、`RUN_DB_TESTS=1 npm test`（38 个测试文件、167 项测试）、`npm run build`、`npm audit --omit=dev`、Compose 配置、App/Worker Docker 构建全部通过。
- Prisma migration 状态通过（34 个 migration），`npm run data:reconcile` 返回 0 项异常。
- 数据库集成覆盖并发抽奖、积分幂等、库存不超卖、商品领奖/过期/撤回、祝福唯一性、投稿加成、申诉与撤销，共 12 项生日集成测试通过。
- Playwright 共 29 项通过；生日路径在 `390x844` 和 `1440x900` 验证首次权益说明、登记、待生效、隐藏、生日墙、无寿星、抽奖、领奖、祝福、错误和加载状态。
- 隔离 PostgreSQL/Redis staging 等效环境完成 migration、数据库测试、对账和双尺寸 UI 验收；未使用生产成员数据。

## 开关与运营状态

- `BIRTHDAY_PROGRAM`：已开启。
- `BIRTHDAY_REWARDS`：关闭。
- 2026-08-07 20:58（Asia/Shanghai），生产管理员在管理后台手动开启“生日星愿”，成员可以登记和完善生日资料、进入生日墙并使用祝福功能；操作经过现有服务端 RBAC 与审计。
- “生日奖励发放”未开启，因此抽奖、生日投稿积分加成和商品领奖仍暂停，不产生奖励积分或占用奖池库存。
- 上线前置条件仍为管理员配置合规商品奖池、核对生日预留库存，并确认商品不是现金类。
- 奖池前置条件完成后再开启 `BIRTHDAY_REWARDS`；开启前继续验收登记、隐私和生日墙，且不会产生生日积分或奖品。

## 生产验收与观察

- 部署 workflow 于 2026-08-07 20:16:24 开始，20:22:43 完成；发布提交 ancestry、发布前健康、不可变镜像 revision、migration、Web、Worker 和 Ingress 步骤全部通过。
- App commit：`b317f95c2d7aa57e3d489714ba712c23efc58e5f`。
- Worker commit：`b317f95c2d7aa57e3d489714ba712c23efc58e5f`。
- Database、Redis、Worker：`ok`；主业务队列 waiting、active、failed 均为 0。
- 观察窗口：2026-08-07 20:22:43–20:53:37（Asia/Shanghai），共 30 分 54 秒。
- 发布 workflow 初始健康检查、窗口内定期采样与结束采样全部通过；App/Worker SHA、Database、Redis、Worker 和主业务队列持续正常，主业务队列 waiting、active、failed 始终为 0。
- 周挑战队列 failed 始终为 2 条历史任务，没有新增；观察期间未触发回滚。

## 已知边界与回滚

- 健康接口的 `degraded=true` 来自发布前已存在的周挑战周期 `FAILED`，其 `failureReason` 为 `This operation was aborted`；本次生日发布未改变该状态。
- 发布前的 [Production Daily Operations Check #31136287411](https://github.com/Jin070810/miaomiaojianjituan/actions/runs/31136287411) 因同一周挑战异常及连续模型调用失败而失败；该检查同时确认余额差异、重复 `photoId`、异常订单均为 0，Redis、Worker 和主队列正常。
- 周挑战主周期保留 0 个 assignment 和 8 次历史 attempt；周挑战队列保留 1 个未来 delayed job 和 2 个历史 failed job。主业务队列无失败任务。
- 生日开关保持关闭，因此当前不需要停止投稿、积分、商城、排行榜或个人中心。
- 若启用后发现积分错账、库存异常、越权或敏感生日泄露，先关闭 `BIRTHDAY_REWARDS`，再关闭 `BIRTHDAY_PROGRAM`，保留积分、奖品、订单、祝福与审计记录。
- 应用通过受控 `Deploy Production` workflow 同时回滚 App/Worker 到 `v1.8.5`；四个追加 migration 不逆向删除，错账通过审计补偿，结构问题通过前向 migration 修复。

## 生日基础入口启用

- 2026-08-07 20:58（Asia/Shanghai），生产管理员在管理后台“运营开关”中手动开启 `BIRTHDAY_PROGRAM`；该操作经过服务端 RBAC、同源校验、事务和审计记录。
- `BIRTHDAY_REWARDS` 继续保持关闭，等待生产奖池配置和预留库存核对。
- 成员可开始自主登记生日；首次登记在 7 天后生效，延迟期间发生的生日不补发权益。
